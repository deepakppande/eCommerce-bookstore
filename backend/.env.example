PORT=3000
NODE_ENV=development

# PostgreSQL — change 'postgres' password to match what you set during install
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ebookstore

# JWT
JWT_SECRET=ebookstore_jwt_secret_change_in_prod
JWT_EXPIRES_IN=7d
